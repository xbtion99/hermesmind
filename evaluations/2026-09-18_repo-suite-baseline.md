# 2026-09-18 평가: 저장소 전체 테스트 스위트 대비 회귀 여부

## 평가 대상

- 브랜치 `claude/abstract-deep-writing-program-3q05pn` (커밋 fff541b)
- 기준선: `origin/main` (커밋 a7e7921)

## 평가 기준

AGENTS.md는 푸시 전 전체 스위트 실행을 요구한다. 그동안 `outputs/abstract-writer/`의
자체 테스트(156개)만 돌렸고 저장소 스위트는 돌리지 않았다. 이 브랜치가 저장소 테스트를
깨뜨렸는지 확인한다.

## 실제 확인 방법

`scripts/run_tests.sh`는 venv가 없으면 중단하므로, 스크립트의 위생 환경(TZ=UTC,
LANG=C.UTF-8, PYTHONHASHSEED=0, `-n 4`)을 그대로 재현해 실행했다.
`git worktree`로 `origin/main`을 같은 샌드박스에 펼쳐 같은 명령으로 비교했다.

| 실행 | 실패 | 통과 | 오류 |
|---|---|---|---|
| main (a7e7921) | 2977 | 11060 | 935 |
| 브랜치 1회차 | 2978 | 11059 | 935 |
| 브랜치 2회차 | 2977 | 11060 | 935 |

영향 범위를 좁힌 비교(스킬 스캔·저장소 트리 열거 테스트 519개):
양쪽 모두 `7 failed, 512 passed, 7 skipped, 1 error`로 완전히 동일.

## 통과한 점

- 브랜치와 main의 결과가 일치한다. 이 변경은 저장소 테스트를 깨뜨리지 않는다.
- 1회차의 1건 차이는 2회차에서 사라졌다. 흔들리는 테스트이지 회귀가 아니다.
- 새 스킬 파일이 스킬을 스캔하는 테스트 157개를 통과한다.

## 발견한 문제와 심각도

| 문제 | 심각도 | 비고 |
|---|---|---|
| 샌드박스에 저장소 선택적 의존성 없음 → 2977 실패, 276 수집 오류 | 높음(환경) | 내 변경과 무관. main도 같다. CI에서는 `.[all]`이 설치되므로 다를 수 있다 |
| 이 스위트에 흔들리는 테스트가 최소 1개 있다 | 낮음 | 같은 커밋에서 실패 수가 2978↔2977로 변동 |
| venv가 없어 `scripts/run_tests.sh`를 쓰지 못함 | 중간 | 환경을 손으로 재현했다. CI 완전 동치는 아니다 |

## 이전 평가 대비

2026-09-17 평가까지는 저장소 스위트가 미측정이었다. 이제 기준선 대비 회귀 없음이 확인됐다.
다만 절대적 통과가 아니라 **main과 동일함**을 보인 것이다.

## 다음 개선 우선순위 한 가지

의존성이 갖춰진 환경(CI 또는 사용자 Mac)에서 같은 비교를 한 번 더 해
2977개 실패가 정말 환경 탓인지 확인한다.
