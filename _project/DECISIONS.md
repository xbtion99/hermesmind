# DECISIONS

| 날짜 | 결정 | 근거 |
|---|---|---|
| 2026-09-16 | "깊이"를 여섯 원리(구별·닻·긴장·결과·전환·감시 어휘)로 정의하고 `outputs/abstract-writer/METHOD.md`를 정본으로 둔다 | 추상 사다리(Hayakawa)와 반박 가능성 기준을 검사 가능한 규칙으로 옮긴 것. research/2026-09-16 참고 |
| 2026-09-16 | 파이프라인은 발굴→작성→검사→수정 순서를 고정하고, "먼저 쓰고 나중에 깊이를 더하는" 순서를 금지한다 | 깊이는 구별·긴장·닻이 먼저 있어야 생긴다. 사후 첨가는 장식이 된다 |
| 2026-09-16 | 프로그램은 `outputs/abstract-writer/`에 의존성 없는 독립 패키지로 두고, Hermes 통합은 얇은 스킬 한 장으로만 한다 | 사용자 운영 지침(outputs/ 정본) 준수. 대형 Hermes 코드베이스에 결합하지 않아 단독 실행·테스트 가능 |
| 2026-09-16 | 모델 프로바이더는 chat-completions 호환 엔드포인트만 지원한다 (stdlib urllib) | Hermes 사용자 대부분이 OpenRouter/Nous Portal 등 호환 엔드포인트를 씀. SDK 의존성 0 유지 |
| 2026-09-16 | 감사(audit) 점수 평균은 모델 값이 아니라 재계산한다. 린트 통과도 기본 수락 조건에 포함한다 | 모델의 자기 채점 부풀림 방지, 결정론적 안전망 |
| 2026-09-16 | 폰 사용 경로로 Termux를 채택하고, `aw` 명령은 cd 대신 PYTHONPATH로 구현한다 | 사용자가 Termux를 선택. cd 방식은 `--out piece.md` 같은 상대경로가 패키지 폴더에 저장되는 footgun이 있었다 |
| 2026-09-17 | 함축을 형식(form)이나 길이(length)가 아니라 별도의 레지스터 축으로 둔다 | 여섯 원리는 글이 무엇을 갖출지를 정하고, 레지스터는 그것을 말할지 말지를 정한다. 두 축은 독립적이라 essay/fragments/letter 어디에도 함축을 걸 수 있다 |
| 2026-09-17 | scaffold_density는 plain에서 기록만 하고 compressed에서만 지적한다 | 접속사와 자기 해설은 plain에서는 미덕이고 compressed에서만 결함이다. 같은 지표를 레지스터에 따라 다르게 해석한다 |
| 2026-09-17 | 셸은 비ASCII만 거르고 한글 판별은 Python이 한다 | bash 글로브 `[가-힣]`는 바이트 범위라 café도 매칭됨(실측). 오타에 API를 호출해 비용이 나가는 것을 막아야 함 |
| 2026-09-17 | 수식어가 겹치면 마지막에 친 것이 이긴다 | "짧게 길게"는 자기 정정으로 읽는 것이 자연스러움 |
| 2026-09-17 | .bashrc는 마커로 감싼 구간을 통째로 교체한다 | "없으면 추가" 방식은 기능이 늘어도 예전 설치본이 갱신되지 않아 사용자 폰에서 훅이 설치되지 않았다. 교체 방식이라야 업그레이드가 된다 |
| 2026-09-17 | 셸 스크립트의 텍스트 편집 로직을 Python 모듈로 옮긴다 | grep 기반 분기는 테스트가 안 붙어 이번 실패를 사전에 못 잡았다. shellrc.py는 단위 테스트 13개로 덮었다 |
| 2026-09-17 | 키 파일을 실행 시점에 직접 읽는다 | 폰에서 nano로 키를 넣고 바로 실행하는 것이 자연스럽다. 환경변수만 보면 source나 재로그인이 한 단계 더 필요하다 |
| 2026-09-17 | $HERMES_HOME/.env를 키 폴백으로 읽는다 | 이 저장소가 Hermes 포크이고 같은 기기에 이미 키가 있을 수 있다. 경로 규약은 hermes_constants.get_env_path()와 같다 |
| 2026-09-17 | 엔드포인트와 모델을 키에서 추론한다 | 키 한 줄만 넣는 것이 폰에서 유일하게 현실적인 절차다. 키와 엔드포인트가 어긋나면 401만 떠서 원인을 알 수 없다 |
| 2026-09-17 | 기본값과 같은 값은 "사용자가 정한 것"으로 보지 않는다 | 이미 배포된 키 파일 템플릿이 OpenRouter 기본값을 export로 박아 두었다. 그대로면 자동 선택이 영원히 막힌다 |
| 2026-09-17 | 키 없이 쓰는 --prompt 경로를 넣는다 | ChatGPT 구독과 API 키는 별개 상품이라 연동이 불가능하다. 결제 없이 방법론을 쓸 수 있어야 프로그램이 지금 쓸모가 있다 |
| 2026-09-17 | 한글 단어 경로는 키가 없으면 프롬프트로 대체한다 | 최소 타이핑이 목적인 인터페이스가 오류만 내면 쓸모가 없다. 명령을 다 쓰는 쪽은 그대로 오류를 내 예측 가능성을 남긴다 |
